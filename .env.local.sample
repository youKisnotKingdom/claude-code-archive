DEV_FE_PORT=3400
DEV_BE_PORT=3401

# Authentication password
CCV_AUTH_PASSWORD=your-secure-password-here

# Server hostname (use 0.0.0.0 to bind to all interfaces for remote access)
# HOSTNAME=localhost
