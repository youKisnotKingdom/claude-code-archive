# Upstream GitHub Workflows

These workflow files were part of the upstream project snapshot. They are kept
here for reference only and are intentionally outside `.github/workflows/` so
they do not run in this private archive.
