# GitHub Actions

This private archive intentionally has no active GitHub Actions workflows.

The upstream workflow files are preserved under `docs/upstream-github-workflows/`
for reference only. Do not add active workflows here unless this archive is
explicitly being converted into a maintained GitHub project.
